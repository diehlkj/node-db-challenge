const express = require('express');
const Tasks = require('./tasksDb.js');
const router = express.Router();

// /api/tasks
    // get
    // post
// /api/tasks/:id
    // put      -- optional
    // delete   -- optional