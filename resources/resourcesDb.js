const db = require('../data/dbConfig.js');

module.exports = {
    find,
    findById,
    insert
}
// /api/resources
    // get
    // post
// /api/resources/:id
    // put      -- optional
    // delete   -- optional